import { RegisterLoginDto } from './register-login-dto.model';

describe('RegisterLoginDto', () => {
  it('should create an instance', () => {
    expect(new RegisterLoginDto()).toBeTruthy();
  });
});
